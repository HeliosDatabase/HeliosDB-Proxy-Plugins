[package]
name        = "helios-plugin-audit-chain"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
description = "Hash-chained tamper-evident audit log; compatible with RFC-3161 timestamping"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
